apiVersion: apps/v1
kind: Deployment
metadata:
  name: fileupload
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fileupload
  template:
    metadata:
      labels:
        app: fileupload
    spec:
      containers:
      - name: fileupload
        image: personicleregistry.azurecr.io/fileupload
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
      - env:
        - name: PORT
          value: "3000"
        envFrom:
        - configMapRef:
            name: fileupload
        
---
apiVersion: v1
kind: Service
metadata:
  name: fileupload-svc
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  type: LoadBalancer
